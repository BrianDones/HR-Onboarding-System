# HR-Onboarding-System
This system is designed to utilize AWS API Gateway and backend Lambda functions to emulate an enivornment where newly added employee information is stored in a database (DynamoDB). An integration is also configured between the API Gateway and company Slack Workspace to ensure new employees are automatically invited to the Slack workspace.
